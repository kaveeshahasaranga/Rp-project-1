# TranSalNet Model Weights

Place `TranSalNet_Res.pth` here to enable GPU-accelerated saliency prediction.

Download from:
https://github.com/LJOVO/TranSalNet (see README for Google Drive link)

If weights are absent, the system automatically falls back to OpenCV Spectral Residual Saliency (CPU-based).
